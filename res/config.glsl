#ifndef CONFIG_GLSL
#define CONFIG_GLSL

#include "config.h"

const vec3 sky_color = vec3(0.8, 0.9, 1.0);
const vec3 crosshair_color = vec3(1.0, 1.0, 1.0);
const float crosshair_width = 0.01;
const float crosshair_thickness = 0.002;
const float block_left = 0.01;
const float block_bottom = 0.01;
const float block_width = 0.1;
const float world_fog_distance = 400.0;
const float world_fog_factor = 2.5;
const float raycast_size = 1.05;
const float raycast_alpha = 0.2;

#endif